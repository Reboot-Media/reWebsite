import React from 'react'
import ReactDOM from 'react-dom/client'
import PreCallPage from './PreCallPage.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PreCallPage />
  </React.StrictMode>,
)
